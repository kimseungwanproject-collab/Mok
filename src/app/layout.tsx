import type { Metadata } from "next";
import { Inter, Montserrat } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const montserrat = Montserrat({
  variable: "--font-montserrat",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MOK Contracting LLC | DMV Region's Trusted Buildout Experts",
  description: "Specializing in commercial tenant spaces, retail store buildouts, and restaurant fit-outs across the DMV region. Over 150 successful projects completed.",
  keywords: "Tenant buildout contractor DMV, Restaurant construction Virginia, Retail store buildout, Commercial office renovation, Virginia Class A contractor, DMV commercial construction",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body
        className={`${inter.variable} ${montserrat.variable} font-sans antialiased min-h-screen flex flex-col`}
      >
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
